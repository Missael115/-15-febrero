function setup() {
  createCanvas(510, 510);
}

function draw() {
  background(200);
  for (var x = 10; x <= 500; x = x + 10) {
    for (var y = 10; y <= 500; y = y + 10)
      ellipse(x, y, 10, 10);
    fill(25, 255, 0);
  }
}
